_target_: src.data.mhspa_datamodule_ssl.MHSPADataModule
data_dir: ${paths.data_dir}
data_name: detections_adquired_v22.h5 # Needs to be divisible by the number of devices (e.g., if in a distributed setup)
num_workers: 16
pin_memory: True
batch_size: 256
